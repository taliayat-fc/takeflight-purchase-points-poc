# Disabled until AWS credentials are configured
# To enable: Remove this file and restore from git history

name: Deploy to AWS S3 (DISABLED)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: false  # Disabled
    
    steps:
      - name: Placeholder
        run: echo "AWS deployment disabled - using Vercel for POC"
